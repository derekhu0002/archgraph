# ArchGraph 开发者社区 — 架构复审（修订版）

> 工作包：开发者社区开发交付（2751）
> 复审人：laodong（系统架构师，sessionId 88bdf28a-55eb-48d8-9092-a01ecfb6a681）
> 复审对象：修订后的方案设计 `docs/developer-community-design.md`（commit ec82700）+ 新增贡献指南 `docs/developer-community-guide.md`
> 前次评审：`docs/developer-community-review-architecture.md`（结论「需修改」，提出 4 点反馈）

## 1. 结论

**通过**。

前次评审提出的 4 点反馈（2 必改 + 2 建议）均已落实：发布脱敏控制以「检查清单（必做）+ 敏感扫描脚本（可选）」落地，下载导入前置格式/大小校验已明确并写入 AC-4 与验收用例，平台依赖边界与大小分级策略已记录并回写到图谱 2748/2752，安全验收用例 AC-6 与 AT-2751-04/05 已登记且可执行。回归测试 `tests/developer-community.test.js` 5/5 通过。

## 2. 逐项核对结果

| # | 前次反馈 | 结果 | 说明 |
| --- | --- | --- | --- |
| ① | 发布脱敏控制（必改） | **已解决** | 贡献指南新增「发布前脱敏检查清单」，覆盖密钥/token/密码、内部绝对路径、commit 详情、个人信息、内部备注 5 类敏感项，并明确「必须逐项核对」；设计 §5 同步描述，辅助发布脚本内置敏感扫描与告警列为可选（与前次反馈「可选」定位一致）。 |
| ② | 下载格式校验（必改） | **已解决** | 设计 §6 与指南 §4 明确 AC-4 导入前置条件：子图 JSON 须符合 `export-to-kg.js` 导出格式（`elements`/`relationships`/`views`）且不超过大小上限，否则拒绝导入；AC-4 验收映射已更新，并新增可执行验收用例 AT-2751-05。 |
| ③ | 平台依赖边界与大小策略（建议） | **已解决** | 设计 §2.2 显式记录供应商锁定/单点依赖边界及「Gist 约 10MB 上限 → 改用仓库 `KGlibrary/` 文件承载」的大小分级策略；并回写 2748/2752 元素描述，为将来替换载体保留语义锚点。 |
| ④ | 安全验收用例（建议） | **已解决** | 需求侧新增安全验收 AC-6（发布脱敏），图谱 2751 工作包登记 AT-2751-04-发布前脱敏、AT-2751-05-下载导入格式校验 两条 Acceptance Test，GIVEN-WHEN-THEN 格式、可执行（`tests/developer-community.test.js`），status 置为 DESIGN_REVISED。 |

## 3. 遗留问题

无。

（附注：敏感扫描脚本仍为「可选」，与前次反馈定位一致，不构成阻断；格式/大小校验在方案层已作为强制前置条件明确定义，其运行时强制执行属后续实现阶段范畴，不在本设计复审阻塞项内。）
